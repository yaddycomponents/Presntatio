# Presntatio

Motion-graphic release presentation for the **Heisenberg / `new-bundle`** modernization
(React + Vite + Framer Motion).

> ⚠️ **Sample / work-in-progress.** Content and numbers will be updated and rectified before
> the final `final.mp4` render. See [`app/SKILL.md`](app/SKILL.md) for how to run it, add scenes,
> and export to video.

## Quick start
```bash
cd app
npm install
npm run dev      # http://localhost:5173   (add /?play for autoplay)
```
